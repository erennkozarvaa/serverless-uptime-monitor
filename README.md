# serverless-uptime-monitor
Passed my AWS CCP and figured it was time to actually build something instead of just collecting badges. This is a serverless uptime monitor I'm putting together with EventBridge, Lambda, DynamoDB, and SNS — learning by doing.
